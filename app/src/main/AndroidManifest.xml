<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.children.greendaoapp">

    <application
        android:name="com.children.greendaoapp.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.GreenDaoApp">

        <activity
            android:name=".ui.LoginActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.RegistActivity"
            android:exported="false" />
        <activity
            android:name=".ui.AddUserActivity"
            android:exported="false" />
        <activity
            android:name=".ui.UserInfoActivity"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="false" />
        <activity
            android:name=".ui.InfoManagerActivity"
            android:exported="false" />
        <activity
            android:name=".ui.ModifyPwdActivity"
            android:exported="false" />
    </application>

</manifest>